# FSC-147 数据集使用说明

本文档说明如何在本仓库中使用 FSC-147 数据集进行类别无关的少样本目标计数。

## 什么是 FSC-147？

FSC-147（Few-Shot Counting）是一个专门为少样本目标计数任务设计的数据集。与 COCO 不同，FSC-147 是专门为计数任务构建的：

- **6,135 张图像**，包含多种目标类别
- 每张图像有 **3 个示例边界框**（参考目标）
- 每个目标实例都有**点标注**
- **预定义的训练/验证/测试划分**
- **类别无关设计** - 不需要类别标签

## 数据集下载

1. 访问官方仓库：https://github.com/cvlab-stonybrook/LearningToCountEverything
2. 按照说明下载 FSC-147_384_V2.zip
3. 将数据集解压到您选择的位置

## 数据集结构

下载后的 FSC-147 目录结构应如下：

```
FSC147_384_V2/
├── images_384_VarV2/
│   └── *.jpg (6135张384x384分辨率的图像)
├── annotation_FSC147_384.json
├── Train_Test_Val_FSC_147.json
└── ImageClasses_FSC147.txt
```

## 配置

### 第一步：更新配置文件

编辑 `configs/config_fsc147.yaml`，更新 `data_path` 为您的数据集路径：

```yaml
data:
  dataset: fsc147
  data_path: /your/path/to/FSC147_384_V2  # 更新为您的路径！
  num_references: 3
```

### 第二步：配置训练参数（可选）

您可以在同一配置文件中调整训练参数：

```yaml
train:
  epochs: 5
  batch_size: 12
  num_workers: 2
  ssim_loss: 1.0e-5
  references: 3
  result_path: ./results
```

## 在 FSC-147 上训练

### 基本训练

```bash
python main.py --config=config_fsc147.yaml --doc=fsc147_exp --train
```

训练日志和模型检查点将保存到 `exp/logs/fsc147_exp/`

## 在 FSC-147 上测试/评估

### 第一步：设置检查点路径

编辑 `configs/config_fsc147.yaml`，更新检查点路径：

```yaml
eval:
  checkpoint: ./exp/logs/fsc147_exp/model_epoch_4.pth  # 更新为您的模型路径！
  sample: true
  image_folder: ./eval_results
```

### 第二步：运行评估

```bash
python main.py --config=config_fsc147.yaml --doc=fsc147_test --test
```

评估将：
- 使用 FSC-147 预定义的**测试集划分**
- 计算 MAE（平均绝对误差）和 MSE（均方误差）
- 如果 `sample: true`，可选择保存可视化样本

## COCO 和 FSC-147 的主要区别

| 特性 | COCO 数据集 | FSC-147 数据集 |
|------|------------|----------------|
| 参考图像 | 每类别500张裁剪图 | 每张图像3个示例框 |
| 标注方式 | 边界框+分割 | 点标注 |
| 预处理 | 需要运行 crop.py | 无需预处理 |
| 样本数 | 5-shot（可配置） | 3-shot（原生） |
| 类别 | 80个目标类别 | 类别无关 |
| 训练划分 | 自定义折叠 | 预定义划分 |

## 快速开始示例

```bash
# 1. 下载 FSC-147 数据集
# 2. 更新配置文件
nano configs/config_fsc147.yaml  # 设置您的 data_path

# 3. 训练
python main.py --config=config_fsc147.yaml --doc=quickstart --train

# 4. 测试
# 首先，在 config_fsc147.yaml 中更新 checkpoint 路径
python main.py --config=config_fsc147.yaml --doc=quickstart_test --test
```

## 数据加载器说明

FSC-147 数据加载器（`data/fsc147.py`）自动处理：

1. 从 `annotation_FSC147_384.json` 加载标注
2. 从每张图像的3个边界框中提取示例区域
3. 从点标注生成密度图
4. 对查询图像和参考图像应用变换
5. 自动管理训练/验证/测试划分

### 数据格式

每个批次包含：
- **查询张量**：`(batch_size, 3, 256, 256)` - 需要计数目标的图像
- **参考张量**：`(batch_size, 3, 3, 64, 64)` - 3个示例裁剪
- **密度图**：`(batch_size, 1, 256, 256)` - 真实密度图

## 故障排除

### 问题："Failed to load image"
- 检查配置文件中的 `data_path` 是否指向正确的目录
- 验证 `images_384_VarV2/` 子目录是否存在

### 问题：ModuleNotFoundError
- 运行 `pip install -r requirements.txt`
- 确保所有依赖项都已安装

### 问题：内存不足
- 在 config_fsc147.yaml 中减少 `batch_size`
- 使用更少的 `num_workers`

## 进一步阅读

- **FSC-147 论文**："Learning To Count Everything" (CVPR 2021)
- **原始论文**："Class-Agnostic Few-Shot Object Counting" (WACV 2021)
- **英文详细说明**：[FSC147_USAGE.md](FSC147_USAGE.md)

## 支持

如果遇到 FSC-147 数据集相关的问题：
1. 检查您的数据集结构是否与预期格式匹配
2. 验证 JSON 文件是否有效且包含预期字段
3. 确保图像在正确的目录中

对于一般模型或训练问题，请参考主 README.md
